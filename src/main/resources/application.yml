spring:
  datasource:
     username: root
     password: rootpassword
     url: jdbc:mysql://localhost:3306/transport
     driver-class-name: com.mysql.jdbc.Driver
     
  jpa:
     hibernate:
      ddl-auto: update
     show-sql: true